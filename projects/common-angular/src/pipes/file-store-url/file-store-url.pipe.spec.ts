import {FileStoreUrlPipe} from './file-store-url.pipe';

describe('FileStoreUrlPipe', () => {
  it('create an instance', () => {
    const pipe = new FileStoreUrlPipe();
    expect(pipe).toBeTruthy();
  });
});
